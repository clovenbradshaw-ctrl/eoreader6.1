# eoreader6.1 · kernel · turn 008 — REFUSED, not earned
# Attempted act: INS*  (Instance aimed at Pattern)   Stance: Composing   Site: Kind
#
# INS's third use would compose anchored entities into something that
# exceeds either alone — but composing requires at least two anchors to
# compose. Turn-7 minted exactly one: @444b54f3c0bfb037, the "index:16 of
# 32, absent" entity. There is nothing else anchored yet to compose it
# with.
#
# This is structurally the same refusal gap-002 recorded for NUL*: an act
# whose Decal-table meaning is self-application/composition-over-recurrence
# cannot be performed on a sample of one, no matter how solid that one
# instance is. Solidity of a single entity is exactly what INS+ (turn-7)
# already established, checkably (kernel/data/anchor-001.json). It is not
# what INS* asks for.
#
# What would earn it: a second entity, independently anchored the same
# way (its own SHA-256 digest over its own earned content — not this one
# copied), that turn-7's entity could be meaningfully composed with. Nothing
# in this kernel currently proposes what that second entity would be, and
# manufacturing one only to close this cell would be exactly the kind of
# un-earned generality eoreader6's SEED.md #1 refuses for priors.

:gap-008  eot:opens        :turn-7 .
:gap-008  eot:attempted    eot:INS* .
:gap-008  eot:refused      true .
:gap-008  eot:reason       "insufficient_entities_to_compose" .
:gap-008  eot:witnessed    false .
